<div class="container mt-4">
    <h1 class="text-center mb-4">{{disco.titulo}}</h1>
    <div class="row">
        <div class="col-md-6">
            <img src="/uploads/{{disco.capa}}" class="img-fluid rounded shadow-sm" alt="{{disco.titulo}}">
        </div>
        <div class="col-md-6">
            <p><strong>Ano de Lançamento:</strong> {{disco.anoLancamento}}</p>
            <p><strong>Gênero:</strong> <span class="badge bg-primary">{{disco.generoMusical}}</span></p>
            <hr>
            <h6>Faixas:</h6>
            <ul class="list-group">
                <li class="list-group-item">
                    <strong>{{disco.faixaNome}}</strong> <span class="text-muted">({{disco.faixaDuracao}})</span>
                    <audio controls class="w-100 mt-2">
                        <source src="/uploads/{{disco.faixaAudio}}" type="audio/mpeg">
                        Seu navegador não suporta o player de áudio.
                    </audio>
                </li>
            </ul>
            <hr>
            <h6>Artista:</h6>
            <p>{{disco.nomeArtista}}</p>
            <div class="d-flex gap-2">
                <a href="/discos/editar/{{disco.discoId}}" class="btn btn-warning">Editar</a>
                <form action="/discos/excluir/{{disco.discoId}}" method="POST" onsubmit="return confirm('Tem certeza que deseja excluir este disco?');">
                    <button type="submit" class="btn btn-danger">Excluir</button>
                </form>
            </div>
        </div>
    </div>
</div>
